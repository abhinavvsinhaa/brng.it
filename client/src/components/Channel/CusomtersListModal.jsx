import React, { useState } from 'react';

const CustomersListModal = ({ visible }) => {
  

  return (
    <>
      <Button type="primary" onClick={showModal}>
        Open Modal
      </Button>
      
    </>
  );
};

export default CustomersListModal;